<%= render(PageHeadingComponent, title: t('.page_title'), subtitle: t('.page_subtitle')) %>
<% @resources.each do |resource| %>
  <%= render(ResourceCardComponent, resource: resource) %>
<% end %>
<%= link_to 'New Resource', new_resource_path %>
